4.1 列属性
PK:是否为主键
NN:是否能为空值
UQ:是否为独一无二的值
B:是否以二进制表示
UN:是否是有符号的值
ZF:是否自动填充0
AI:是否自增
G:是否生成列
Datatype:即数据类型，如：VARCHAR是可变字符类型
                        如果是VARCHAR(5)就代表最多接受5个字符，如果不足5个就不预留更多的内存
4.2 INSERT INTO 插入数据 
//插入数据的前提是列和列对齐
USE sql_store;
INSERT INTO customers (first_name,last_name,birth_date,address,city,state)
VALUES ('John','Smith',NULL,'aaa','city','CA')

USE sql_store;
INSERT INTO shippers (name)
VALUES ("1"), //如果想要插入多个数据的话，只需要在VALUES下继续添加数值即可，每个数值之间用()隔开
       ('2')

4.3 插入分层行
？
USE sql_store;
INSERT INTO orders(customer_id , order_date, status)
values(1,"1992-01-01" , 1);

INSERT INTO order_items
VALUES (LAST_INSERT_ID(),1,1,3.95)//那个函数的意思是获取最新的主键ID

4.4创建表复制
把一个表的数据移到另一个表里，可以是部分，也可以是全部，在WHERE添加即可
INSERT INTO order_archived
SELECT *
FROM orders
WHERE order_date < "2019-01-01"

4.5 CREATE TABLE ... AS 新建表
//新建的表一定起码要有一列
CREATE TABLE order_archived AS
SELECT * FROM orders
